import 'package:flutter/material.dart';

class CourseListView extends StatelessWidget {
  const CourseListView({super.key});

  // TODO: Implement the course list view.
  // Add placeholder content for now to visualize the view in the app.
  // Consult the Flutter documentation for ListView and related widgets to create a scrollable list of courses.

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      color: Colors.blue,
      child: Center(child: Text('Course List View')),
    );
  }
}
