import 'package:flutter/material.dart';

class LookupCourseView extends StatelessWidget {
  const LookupCourseView({super.key});

  // TODO: Implement the lookup course view per the design specifications.
  // Bonus points for an autocomplete dropdown on the Prefix field
  // Consult the Flutter documentation for TextField, DropdownButton, and related widgets to create a search form with the specified fields.

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      color: Colors.orange,
      child: Center(child: Text('Lookup Course View')),
    );
  }
}
